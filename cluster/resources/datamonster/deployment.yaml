apiVersion: apps/v1
kind: Deployment
metadata:
  name: datamonster
  namespace: datamonster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: datamonster
  template:
    metadata:
      labels:
        app: datamonster
    spec:
      imagePullSecrets:
        - name: acr-credentials
      containers:
        - name: datamonster
          image: batcomputer-gse9eyezfzhubya2.azurecr.io/datamonster-web:latest
          ports:
            - name: http
              containerPort: 80
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 32Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: datamonster
  namespace: datamonster
spec:
  selector:
    app: datamonster
  ports:
    - name: http
      port: 80
      targetPort: 80
