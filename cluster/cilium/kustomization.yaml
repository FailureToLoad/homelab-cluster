apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: core-cilium
resources:
  - namespace.yaml
helmGlobals:
  chartHome: charts/cilium
helmCharts:
  - name: cilium
    repo: https://helm.cilium.io/
    version: 1.18.0
    releaseName: cilium
    namespace: core-cilium
    valuesFile: resources/values.yaml
